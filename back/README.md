## バックエンド

### npmをつかえ
```
$ npm install -g npx
$ npm install
$ ./start.sh
```

### eslintをつかえ
eslintでairbnbスタイル

### production環境
dockerですぐつくれる
```
$ docker-compose -f docker-compose.yml up -d
```
これでexpressのバックエンドが起動する
